{
  "private": true,
  "name": "invokation",
  "version": "0.4.2-beta1",
  "scripts": {
    "lint": "yarn run lint.lua && yarn run lint.js && yarn run lint.css",
    "lint.lua": "make lint-lua",
    "lint.js": "make lint-js",
    "lint.css": "make lint-css",
    "format": "yarn run format.lua && yarn run format.js && yarn run format.css",
    "format.lua": "make format-lua",
    "format.js": "make format-js",
    "format.css": "make format-css",
    "reset": "yarn run clean && yarn run install && yarn run build",
    "clean": "make clean",
    "install": "make install",
    "build": "make build",
    "launch.game": "make launch-game",
    "launch.tools": "make launch-tools"
  },
  "devDependencies": {
    "@prettier/plugin-lua": "^0.0.1",
    "eslint": "^6.1.0",
    "lua-fmt": "^2.6.0",
    "prettier": "^1.18.2",
    "stylelint": "^10.1.0",
    "stylelint-config-prettier": "^5.2.0",
    "stylelint-config-standard": "^18.3.0",
    "stylelint-order": "^3.0.1"
  }
}
